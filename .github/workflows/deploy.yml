name: Build and Deploy

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:

  validate:
    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Install Dependencies
        run: |
          pip install mkdocs==1.6.1
          pip install mkdocs-material==9.6.14
          pip install pymdown-extensions==10.16.1
          npm install -g markdownlint-cli
          cargo install lychee
          npm ci --include=dev

      - name: Markdown Lint Validation
        continue-on-error: true
        run: |
          markdownlint docs/**/*.md README.md --config .markdownlint.json

      - name: Vale Spell Check
        continue-on-error: true
        uses: errata-ai/vale-action@reviewdog
        with:
          files: docs/
      
      - name: Lint OpenAPI specs
        run: npm run lint:openapi

      - name: Build MkDocs Site
        run: |
          mkdocs build --strict

      - name: Broken Link Validation
        continue-on-error: true
        run: |
          lychee site/**/*.html
  
  openapi-breaking-changes:
    name: OpenAPI Breaking Change Detection
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout PR branch
        uses: actions/checkout@v5

      - name: Check audit log API breaking changes
        uses: oasdiff/oasdiff-action/breaking@main
        with:
          base: https://raw.githubusercontent.com/${{ github.repository }}/${{ github.base_ref }}/docs/openapi/audit-log-api.yaml
          revision: docs/openapi/audit-log-api.yaml

      - name: Check deployment API breaking changes
        uses: oasdiff/oasdiff-action/breaking@main
        with:
          base: https://raw.githubusercontent.com/${{ github.repository }}/${{ github.base_ref }}/docs/openapi/deployment-api.yaml
          revision: docs/openapi/deployment-api.yaml

      - name: Check device API breaking changes
        uses: oasdiff/oasdiff-action/breaking@main
        with:
          base: https://raw.githubusercontent.com/${{ github.repository }}/${{ github.base_ref }}/docs/openapi/device-api.yaml
          revision: docs/openapi/device-api.yaml

      - name: Check remote access API breaking changes
        uses: oasdiff/oasdiff-action/breaking@main
        with:
          base: https://raw.githubusercontent.com/${{ github.repository }}/${{ github.base_ref }}/docs/openapi/remote-access-api.yaml
          revision: docs/openapi/remote-access-api.yaml

      - name: Check session API breaking changes
        uses: oasdiff/oasdiff-action/breaking@main
        with:
          base: https://raw.githubusercontent.com/${{ github.repository }}/${{ github.base_ref }}/docs/openapi/session-api.yaml
          revision: docs/openapi/session-api.yaml

  deploy:
    needs: validate
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      
      - uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm
        
      - name: Install MkDocs
        run: |
          pip install mkdocs==1.6.1
          pip install mkdocs-material==9.6.14
          pip install pymdown-extensions==10.16.1

      - name: Deploy Documentation
        run: |
          mkdocs gh-deploy --force
