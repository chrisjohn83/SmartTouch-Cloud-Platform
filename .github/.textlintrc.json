{
  "rules": {
    "no-todo": true,
    "sentence-length": {
      "max": 300
    },
    "terminology": {
      "terms": {
        "Github": "GitHub",
        "API Endpoint": "API endpoint",
        "Kubernetes Cluster": "Kubernetes cluster"
      }
    }
  }
}
